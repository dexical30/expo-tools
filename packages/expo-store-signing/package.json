{
  "name": "@dexical/expo-store-signing",
  "version": "1.0.12",
  "description": "Expo config plugin for Android store signing",
  "main": "app.plugin.js",
  "scripts": {
    "build": "cd plugin && tsc",
    "clean": "rm -rf plugin/build",
    "test": "cd plugin && jest",
    "prepare": "npm run build",
    "prepublishOnly": "pnpm build"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "files": [
    "plugin/build"
  ],
  "keywords": [
    "expo",
    "expo-plugin",
    "android",
    "signing",
    "keystore"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dexical30/expo-tools.git"
  },
  "author": "dexical30",
  "license": "ISC",
  "dependencies": {
    "@expo/config-plugins": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "expo-module-scripts": "^5.0.7",
    "jest": "^29.0.0",
    "jest-expo": "^52.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "expo": ">=49.0.0"
  },
  "jest": {
    "preset": "jest-expo"
  }
}
